<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Preferencia para el tipo de mapa -->
    <ListPreference
        android:key="map_type"
        android:title="Tipo de mapa"
        android:entries="@array/map_type_entries"
        android:entryValues="@array/map_type_values"
        android:defaultValue="normal" />

    <!-- Preferencia para el idioma -->
    <ListPreference
        android:key="language"
        android:title="Idioma"
        android:entries="@array/language_entries"
        android:entryValues="@array/language_values"
        android:defaultValue="es" />

</PreferenceScreen>
